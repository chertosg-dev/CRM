TSERTOS Insurance CRM
Version: 9.11.4
Build: FORMS_TEMPLATE_DEDUP_PERSISTENT_MAPPING
Date: 2026-09-19

Changes:
- Each PDF template keeps its field mapping, visual positions and font sizes permanently in the Forms Library.
- Re-uploading the exact same PDF is detected by a content fingerprint.
- Duplicate uploads reuse the existing template instead of asking for field mapping again.
- Existing templates are fingerprinted lazily, without deleting saved mappings.
- PDF storage remains on raw bytes for iPhone/Safari reliability.
