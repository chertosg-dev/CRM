# TSERTOS CRM V9.11.22 — Forms Entry Restore

- Restores the **Έντυπα** entry reliably in the top action bar.
- Adds a second **📄 Έντυπα / Συμπλήρωση PDF** entry inside the hamburger menu.
- Uses fallback insertion points if the CRM header structure changes.
- Existing templates, mappings, OCR settings and local data are unchanged.
